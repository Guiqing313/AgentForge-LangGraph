name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-lock.txt
      - name: Ruff
        run: ruff check .
      - name: Pytest
        run: pytest -q
      - name: Freeze environment
        run: pip freeze > pip-freeze.txt
      - name: Upload freeze artifact
        uses: actions/upload-artifact@v4
        with:
          name: pip-freeze
          path: pip-freeze.txt

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare env for compose config
        run: cp .env.example .env
      - name: Build API image
        run: docker build -t agentforge-api:ci .
      - name: Build frontend image
        run: docker build -t agentforge-frontend:ci -f frontend/Dockerfile .
      - name: Validate compose
        run: docker compose config --quiet
